import React from "react";
import Button from "../Button";
import { ButtonProps } from "../Button";

const ButtonStandard = (props: ButtonProps) => {
  return <Button {...props} />;
};

export default ButtonStandard;
